# Security

Do not commit production model artifacts, customer-level scored outputs, credentials, or proprietary source extracts.

Use GitHub Actions secrets or environment-specific secret stores for MLflow credentials and artifact-store access.

Model cards may include business-sensitive performance notes. Keep production model approval evidence in restricted systems when required.
